# Neovim Config

## Setup

- Neovim version: 0.12.2
- Plugin manager: lazy.nvim
- Language: Lua

## Keymaps

### Python

| Mode | Keymap | Action | Plugin / Module | Notes |
|------|--------------|--------------------------|----------------|-------|
| n | `<leader>pc` | Run current Python file | serpy | Opens terminal split and runs current buffer |

## Plugins

- autopairs (automatic bracket/quote pairing)
- blink.cmp (completion engine)
- bufferline.nvim (buffer tabs UI)
- nvim-colorizer (highlight color codes)
- Comment.nvim (smart commenting)
- conform.nvim (formatting pipeline)
- gruvbox.nvim (colorscheme)
- lazydev.nvim (Lua dev helpers)
- lazygit.nvim (Git UI integration)
- mason.nvim (LSP/tool installer)
- mason-lspconfig.nvim (LSP bridge for Mason)
- noice.nvim (UI for cmdline/messages)
- nvim-tree (file explorer)
- nvim-treesitter (syntax parsing/highlighting)
- serpy (custom Python runner plugin)
- lualine.nvim (statusline)
- todo-comments.nvim (TODO/FIXME highlighting)
- toggleterm.nvim (terminal management)
- undotree (undo history visualizer)
- which-key.nvim (keymal hints popup)
